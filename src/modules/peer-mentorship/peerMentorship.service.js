const {
  createRequest,
  getRequests,
  getRequestById,
  respondToRequest,
  acceptHelper,
  resolveRequest,
} = require('./peerMentorship.model');

module.exports = { createRequest, getRequests, getRequestById, respondToRequest, acceptHelper, resolveRequest };
